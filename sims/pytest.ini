[pytest]
testpaths = test
norecursedirs = old-rocket-project venv .git __pycache__ .pytest_cache
# don't want to test old stuff
markers =
    slow: long-running fixed-step integration tests (deselect with -m "not slow")